workspace = { members = ["velvet3D"] }
[package]
name = "rustengine"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
velvet3D = { path = "./velvet3D" }

[lib]
crate-type = ["cdylib", "rlib"]